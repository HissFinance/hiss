{
  "name": "@hiss-finance/mcp-server",
  "version": "0.1.0",
  "description": "Local stdio MCP server for HISS Finance — read-and-prepare tools only. Never exposes keys, never signs, never performs owner-only or admin actions.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "hiss-mcp": "dist/bin/server.js",
    "hiss-mcp-http": "dist/bin/http.js"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "tsx src/bin/server.ts",
    "start:http": "tsx src/bin/http.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hiss-finance/core": "workspace:*",
    "@hiss-finance/sdk": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
